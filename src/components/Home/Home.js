import React from 'react';
import BlogSection from '../BlogSection/BlogSection';

const Home = () => {
    return (
        <div>
            <BlogSection/>
        </div>
    );
};

export default Home;